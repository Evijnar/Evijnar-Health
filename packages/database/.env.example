# Database Connection (PostgreSQL)
# Format: postgresql://user:password@host:port/database
DATABASE_URL="postgresql://evijnar_dev:secure_dev_password@localhost:5432/evijnar_dev"

# Environment
NODE_ENV="development"

# Encryption keys (rotate in production)
ENCRYPTION_KEY_PATIENT_DATA="dev-encryption-key-32-bytes-total"
ENCRYPTION_KEY_PHARMA_DATA="dev-encryption-key-32-bytes-total"

# API Keys (masked from git)
HIPAA_AUDIT_WEBHOOK_URL="https://audit.example.com/webhook"
